<?php
    // change the namespace name with your resource, 
    // if you named it with an other name the framework may not work correctly

    namespace [this_resource];

    // example
    /* class TestController{
        // this method will be used when the http verb is GET
        static function get(){
            return new Response([
                "hello akana"
            ]);
        }
        // this method will be used when the http verb is POST
        static function post(){
            return new Response([
                "hello akana"
            ]);
        }
        // this method will be used when the http verb is PATCH
        static function patch(){
            return new Response([
                "hello akana"
            ]);
        }
        // this method will be used when the http verb is DELETE
        static function delete(){
            return new Response([
                "hello akana"
            ]);
        }
    }*/