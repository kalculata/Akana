<?php
    define('ROOT_ENDPOINT', true);
    define('DEBUG', true);

    //define('ROOT_ENDPOINT_CONTROLLER', NULL);
    define('ROOT_ENDPOINT_CONTROLLER', [
        'file' => '../root_controller.php',
        'controller' => '\App\RootController'
    ]);

    define('APP_RESOURCES', [
        'users'
    ]);
